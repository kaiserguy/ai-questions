<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= (typeof error === 'object' && error.title) ? error.title : 'Error' %> - AI Questions</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
        }
        .error-container {
            background-color: #fff;
            border-radius: 12px;
            padding: 40px;
            margin-top: 50px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            border-left: 4px solid #d32f2f;
        }
        .error-icon {
            font-size: 48px;
            margin-bottom: 20px;
            color: #d32f2f;
        }
        h1 {
            color: #d32f2f;
            margin-bottom: 10px;
            font-size: 28px;
        }
        .error-message {
            margin: 20px 0;
            font-size: 1.1em;
            color: #555;
        }
        .recovery-section {
            background-color: #f5f5f5;
            border-radius: 8px;
            padding: 20px;
            margin: 30px 0;
            text-align: left;
        }
        .recovery-section h2 {
            color: #333;
            font-size: 18px;
            margin-bottom: 10px;
        }
        .recovery-section p {
            color: #666;
            margin: 5px 0;
        }
        .actions {
            margin-top: 30px;
            display: flex;
            gap: 15px;
            justify-content: center;
            flex-wrap: wrap;
        }
        .btn {
            display: inline-block;
            padding: 12px 24px;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.2s;
            cursor: pointer;
            border: none;
            font-size: 16px;
        }
        .btn-primary {
            background-color: #2196F3;
            color: white;
        }
        .btn-primary:hover {
            background-color: #1976D2;
        }
        .btn-secondary {
            background-color: #f5f5f5;
            color: #333;
            border: 1px solid #ddd;
        }
        .btn-secondary:hover {
            background-color: #e0e0e0;
        }
        .error-details {
            margin-top: 30px;
            padding: 15px;
            background-color: #fafafa;
            border-radius: 6px;
            text-align: left;
            font-family: monospace;
            font-size: 12px;
            color: #666;
            max-height: 200px;
            overflow-y: auto;
        }
        .timestamp {
            font-size: 12px;
            color: #999;
            margin-top: 20px;
        }
    </style>
    <link rel="stylesheet" href="/css/toast.css">
</head>
<body>
    <div class="error-container">
        <div class="error-icon" aria-hidden="true">⚠️</div>
        <h1><%= (typeof error === 'object' && error.title) ? error.title : 'An Error Occurred' %></h1>
        <div class="error-message">
            <%= (typeof error === 'object' && error.message) ? error.message : error %>
        </div>
        
        <% if (typeof error === 'object' && error.recovery) { %>
        <div class="recovery-section">
            <h2>What you can do:</h2>
            <p><%= error.recovery %></p>
        </div>
        <% } %>
        
        <div class="actions">
            <button class="btn btn-primary" onclick="history.back()">
                ← Go Back
            </button>
            <a href="/" class="btn btn-secondary">
                🏠 Home Page
            </a>
            <% if (typeof error === 'object' && error.type === 'authentication') { %>
            <a href="/login" class="btn btn-primary">
                🔐 Sign In
            </a>
            <% } %>
            <% if (typeof error === 'object' && error.type === 'ai_service') { %>
            <a href="/config" class="btn btn-secondary">
                ⚙️ Configure Models
            </a>
            <% } %>
        </div>
        
        <% if (typeof error === 'object' && error.details) { %>
        <details>
            <summary style="cursor: pointer; margin-top: 20px; color: #666;">Technical Details</summary>
            <div class="error-details">
                <%= error.details %>
            </div>
        </details>
        <% } %>
        
        <% if (typeof error === 'object' && error.timestamp) { %>
        <div class="timestamp">
            Error occurred at: <%= new Date(error.timestamp).toLocaleString() %>
        </div>
        <% } %>
    </div>
    <script src="/js/toast.js"></script>
</body>
</html>
